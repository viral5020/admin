## Filters

**(Mobile/Desktop Both)**

- Can user enter values in all filter properly ?
- All filter's key pass in api ?

- If api passes more keys then filter,

  - Ask ashish sir is that filter require in that page
  - Or remove that key from api

- Does `Clear` btn gives error in console ?
- Does `Clear` btn clear all filter ?

- Alignment of Filters, `Apply` and `Clear` btn

- Drawer should have Title **Filter** and **cross** icon in mobile
- On clicking `Apply` btn, Drawer should be close in mobile

## Search And Today/All

**(Mobile/Desktop Both)**

- onChange API shuold call

## Table

- Horizontal scroll (in Mobile/Desk)?
- Is there any extra padding (top / bottom of table) ?
- If any table cell has no data, then `-` dash
